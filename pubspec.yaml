name: format_converter
description: "全能格式转换 - 免费离线转换所有文件格式"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  file_picker: ^8.0.0
  permission_handler: ^11.3.0
  share_plus: ^9.0.0
  image: ^4.2.0
  pdf: ^3.11.0
  archive: ^3.5.0
  intl: ^0.19.0
  open_filex: ^4.4.0
  fluttertoast: ^8.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
